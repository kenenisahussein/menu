collections:
  - name: "menu"
    label: "Menu"
    folder: "content"
    files:
      - name: "test"          # changed from "menu"
        label: "Test Menu"
        file: "content/test.json"
        fields:   # keep the same fields as before
